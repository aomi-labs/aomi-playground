# aomi-playground

Sandbox publish repo for the Aomi **portal Deploy tab** demo. Apps land under
`apps/<slug>/` on the `publish` branch; CI builds the cdylib and cuts a release
tagged `apps-<slug>-<short_commit>`. This is a throwaway target — do not host
production apps here.

Platform: `playground` (see `platform.json`).
